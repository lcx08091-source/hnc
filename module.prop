id=hotspot_network_control
name=Hotspot Network Control
version=v3.8.5
versionCode=3805
author=Claude (Anthropic AI)
description=热点设备管控 v3.8.5 · 大 v3.8 阶段 6/6 · netem RTT 语义修复。用户反馈"设置 200ms 延迟,ping 看到 400ms"。根本原因:v3.3.2 以来 set_delay 在 egress(wlan2 下行)和 ingress(ifb0 上行)都加了同样的 netem 延迟,用户心智模型是"ping RTT 增量"但代码是"每方向延迟"。v3.8.5 修复:用户输入的 delay_ms 现在是 RTT 视角,内部除以 2 分给两方向。奇数 egress 向上取整 ingress 向下取整(101→51+50 无精度损失)。实测:设 250ms,ping RTT 从 ~500ms 降到 ~270ms(250 + ~20 base),完美符合预期。jitter 和 loss 保持每方向原值(合并后 RTT jitter ≈ √2 × 单方向,端到端 loss ≈ 2p,精确除法需浮点不值得)。v3.8.5 的语义:delay 是 RTT,jitter/loss 是每方向。改动极小(tc_manager.sh 15 行),但修的是用户每天都能感知的真实 bug,比 WebUI 打磨优先级高。大 v3.8 6/6 阶段完成,HNC 进入稳定维护期。
minKernelSU=11485
