id=hotspot_network_control
name=Hotspot Network Control
version=v3.8.2
versionCode=3802
author=Claude (Anthropic AI)
description=热点设备管控 v3.8.2 · 大 v3.8 阶段 3/6 · B3 调用链真代码集成测试(方案 G: Include + 同名 static 覆盖)。hotspotd.c 底部把 try_mdns_resolve / try_ns_dhcp_resolve / main 三段用 #ifndef HNC_TEST_MODE 包围,前向声明保留在原位置。test_call_chain.c 通过 #include "../hotspotd.c" 加载真代码,自己提供同名 static mock 替换 I/O 函数。测试执行的是 100% 真实的 resolve_hostname 机器码,零 shim 零 drift。27 个真代码集成测试全过(含 v3.7.0 坑 16 + v3.7.2 坑 18 回归保护)。方案选择经历了 E(契约测试,反模式)→ F(--wrap,smoke test 失败)→ G(include + 静态覆盖,成功)三次迭代,每步用 smoke test 验证。生产 hotspotd 符号表和 v3.8.1 完全一致,行为零变化。210 个测试全过。
minKernelSU=11485
