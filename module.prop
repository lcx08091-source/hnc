id=hotspot_network_control
name=Hotspot Network Control
version=v3.8.4
versionCode=3804
author=Claude (Anthropic AI)
description=热点设备管控 v3.8.4 · 大 v3.8 阶段 5/6 · pthread 异步 mDNS worker。HNC 项目首次引入 pthread。scan_arp 和 nl_process 的 re-resolve 路径以前同步调 resolve_hostname,其中 mdns 分支最坏阻塞主循环 800ms,现在改成先同步调 resolve_hostname_dhcp_only(~35ms),只有所有快速路径都失败才 enqueue 到异步 worker。采用 Actor 模型:双队列(工作队列 + 结果队列),单 worker 线程,g_devs 依然主线程独占,零 g_devs 锁。Worker 通过函数指针注入 try_mdns_resolve,不依赖 hotspotd.c 的 static 函数,单元测试可注入 mock。drain_results 的优先级检查(只覆盖 pending 状态)实现了乐观锁,避免 mdns 结果覆盖更高优先级的 dhcp。两轮 Gemini 外部审查:修复 P1(sigdelset 剔除 SIGSEGV/SIGBUS/SIGILL/SIGFPE 避免 POSIX UB)+ 3 个 P2(删除 volatile / strncpy 规范化 / stop 防重入)。31 个 mdns_worker 单元测试 + 268 全套回归测试全过。信号屏蔽 + 双锁不嵌套 + 结果队列有界(16) + Stop 幂等 + join 等最后任务完成。
minKernelSU=11485
