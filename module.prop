id=hotspot_network_control
name=Hotspot Network Control
version=v3.8.1
versionCode=3801
author=Claude (Anthropic AI)
description=热点设备管控 v3.8.1 · 大 v3.8 阶段 2/6 · A3 DHCP/mDNS hostname 持久化 cache(Gemini P1-7 修复)。新增 daemon/hostname_cache.c 独立模块,一旦成功识别设备真名就写入 /data/local/hnc/data/hostname_cache.json,即使 dumpsys ring buffer 滚出或 hotspotd 重启,重连设备依然能立即恢复真名。优先级链新增第 4 级:manual > dhcp > mdns > cache(新) > oui > mac。cache 大小 1024 条上限,LRU 淘汰,原子 tmp+rename 写盘,de-bounce 跟 devices.json 共享写入窗口。WebUI 新增 💾 图标显示"历史缓存识别"。顺手修复 diag.sh 两个假警报(安装目录检查错路径 + hotspotd 防误用过时警告)。37 个 hostname_cache 新单元测试 + 183 历史回归全过。
minKernelSU=11485
