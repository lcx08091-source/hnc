id=hotspot_network_control
name=Hotspot Network Control
version=v3.8.6
versionCode=3806
author=Claude (Anthropic AI)
description=热点设备管控 v3.8.6 · 死代码清理 + 安全加固 + 文档对齐。两份外部 AI 全面审查发现:1) api/server.sh 是 v3.4.11 起就禁用的死代码 692 行,且包含 RCE 风险源(Python http.server + subprocess.run shell=true)。v3.8.6 彻底从仓库删除,消除认知噪音和潜在隐患。2) WebUI 实际通过 KernelSU WebView 桥接(window.ksu.exec)19 处调用,完全不走 HTTP 端口。修正 README.md 把"HTTP API"的过时描述改成"KSU 桥接"。3) hotspotd unix socket 已经是 0600,加 chown(0,0) 纵深防御。4) README.md 版本号从 v3.5.0 / 97 测试 更新到 v3.8.6 / 268 测试。5) service.sh 移除"WebUI URL: http://192.168.43.1:8080"误导日志。6) cleanup.sh 移除 server.sh 进程清理(已不存在)。改动:删除 api/server.sh + 4 个文件改 ~30 行。无功能变化,纯清理。268 测试全过。这是大 v3.8 真正的收尾,HNC 进入稳定 LTS。
minKernelSU=11485
